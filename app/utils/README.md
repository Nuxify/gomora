You may place your utility files in this directory.

A good example of a utility file (utils) are helper functions (or event in a struct).

Tree example:

- utils
    - helpers
        - math
            - math.go
            - math_test.go