The storage directory, aside from your persistent database, can be used to store chunk of logs and files.

A good example for this are logs.

Tree example:

- storage
     - logs
          - http_request.log